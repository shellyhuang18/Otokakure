using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Module;

//This script is only used for testing if other things work.
//Has no actual uses.
public class TestScriptOnly : MonoBehaviour {
	private BaseModule module;
	// Use this for initialization
	void Start () {
//		module = new PitchModule ();
	}
	
	// Update is called once per frame
	void Update () {

	}
}
